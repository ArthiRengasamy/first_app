<h1>Success stories from our Volunteers </h1>

<p style="text-align: right;font-weight:bold;font-style:italic;font-size:1.5em;color:blue" >
  <% if signed_in? %>
      <%= link_to "My success stories", :controller => :users, :action => :show, :id => current_user.id %>
  <% end %>
</p>

<!-- Search implementation using AJAX -->
<%= form_tag stories_path, :method =>'get' do %>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
<%end%>

<!-- <%= will_paginate @stories, :page_links => false%>  -->
<%= will_paginate @stories%>

</br>
</br>


  <% @stories.each do |story| %>
      <tr>
        <td><%= story.content %></td>
        </br>
        </br>
        </br>
        </br>
        </br>
        <p style="text-align: right"><b><%= link_to "Top of page",stories_path %></b></p>

      </tr>

  <% end %>
</table>
<%= will_paginate @stories%>



